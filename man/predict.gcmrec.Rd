% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.gcmrec.R
\name{predict.gcmrec}
\alias{predict.gcmrec}
\title{Predictions from a gcmrec model}
\usage{
\method{predict}{gcmrec}(
  object,
  newdata = NULL,
  type = c("risk", "lp", "survival", "hazard"),
  ...
)
}
\arguments{
\item{object}{An object of class \code{gcmrec}.}

\item{newdata}{Data frame of covariate profiles, one row per profile,
with the covariates used in the model. If \code{NULL}, the profiles of the
data used to fit are taken.}

\item{type}{Type of prediction: \code{"risk"} (relative risk
\eqn{\exp(x'\beta)}, the default), \code{"lp"} (linear predictor
\eqn{x'\beta}), \code{"survival"} or \code{"hazard"} (the whole curve for each
profile).}

\item{...}{Ignored; kept for compatibility with the generic.}
}
\value{
For \code{"risk"} and \code{"lp"}, a numeric vector with one value per
profile. For \code{"survival"} and \code{"hazard"}, a data frame with columns
\code{profile}, \code{time} and \code{estimate}, ready to be plotted (see
\code{\link[=plotPredict]{plotPredict()}}).
}
\description{
Computes the linear predictor, the relative risk or the whole survivor
and cumulative hazard curves for given covariate profiles.
}
\details{
The model is multiplicative, so a covariate profile \eqn{x} scales the
baseline by \eqn{\psi(x) = \exp(x'\beta)}: the survivor function of the
profile is \eqn{S_0(t)^{\psi(x)}} and its cumulative hazard is
\eqn{\psi(x)\Lambda_0(t)}. Note that the curves are functions of the
\strong{effective age}, not of calendar time, and that they describe the gap
to the next event.
}
\examples{
data(readmission)
mod <- gcmrec(Survr(id, time, event) ~ as.factor(dukes) + sex,
              data = readmission, s = 3000)

# Relative risk of two patient profiles
profiles <- data.frame(dukes = c(1, 3), sex = c(1, 1))
predict(mod, profiles, type = "risk")

# Whole survivor curve of each profile
head(predict(mod, profiles, type = "survival"))
}
\seealso{
\code{\link[=plotPredict]{plotPredict()}}, \code{\link[=plot.gcmrec]{plot.gcmrec()}}
}
