% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcf.R
\name{mcf}
\alias{mcf}
\alias{print.mcf}
\alias{plot.mcf}
\title{Mean cumulative function of recurrent events}
\usage{
mcf(data, group = NULL, level = 0.95)

\method{print}{mcf}(x, ...)

\method{plot}{mcf}(x, conf.int = TRUE, ...)
}
\arguments{
\item{data}{A data frame with \code{id}, \code{time} and \code{event} variables, or a
legacy gcmrec list (see \code{\link[=as_gcmrec_data]{as_gcmrec_data()}}).}

\item{group}{Optional grouping variable of length \code{nrow(data)}; one MCF
is estimated per group.}

\item{level}{Confidence level of the pointwise interval.}

\item{x}{An object of class \code{mcf}.}

\item{...}{Ignored; kept for compatibility with the generic.}

\item{conf.int}{Draw the confidence band?}
}
\value{
An object of class \code{mcf}: a data frame with columns \code{time},
\code{estimate}, \code{lower}, \code{upper}, \code{n.risk}, \code{n.event} and, if \code{group} was
given, \code{group}. It has \code{\link[=plot]{plot()}} and \code{\link[=print]{print()}} methods.

For \code{print}, invisibly \code{x}.
}
\description{
Estimates the mean cumulative function (MCF), that is, the expected
number of events accumulated by a subject up to each time. It is the
Nelson-Aalen estimator adapted to recurrent events, and it answers the
question \emph{"how many events does an average subject accumulate?"}
without assuming any model.
}
\details{
The estimator adds, at each event time \eqn{t}, the number of events
observed at \eqn{t} divided by the number of subjects still under
follow-up, and the pointwise variance is accumulated in the same way.
Use it before modelling: comparing the MCF of two groups shows whether
their event burden really differs, and its shape suggests whether the
risk grows or falls with time.
}
\examples{
data(readmission)

# Overall event burden
m <- mcf(readmission)
m
plot(m)

# Does it differ by tumour stage?
m.dukes <- mcf(readmission, group = readmission$dukes)
plot(m.dukes)
}
\seealso{
\code{\link[=graph.caltimes]{graph.caltimes()}} for the individual view of the same data.
}
