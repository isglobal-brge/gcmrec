% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/addCenTime.R
\name{addCenTime}
\alias{addCenTime}
\title{Add a censored time equal to 0}
\usage{
addCenTime(datin, id = 1, time = 2, event = 3)
}
\arguments{
\item{datin}{Data frame containing id, time and event variables; other
covariates are allowed and are copied to the added row.}

\item{id, time, event}{Column positions of the id, time and event
variables in \code{datin}.}
}
\value{
A data frame with an added row (censored time equal to 0) for
those subjects whose end of follow-up coincides with the last
occurrence.
}
\description{
Adds a new row with a censored time equal to 0 for those subjects whose
end of follow-up coincides with the last occurrence (i.e. whose last
record is an event). \code{\link[=Survr]{Survr()}} requires every subject to have exactly
one censored record.
}
\examples{
# The second subject has no censored record: its follow-up ends at the
# last event
dat <- data.frame(
  id    = c(1, 1, 2, 2),
  time  = c(5, 3, 7, 4),
  event = c(1, 0, 1, 1)
)
addCenTime(dat)
}
\seealso{
\code{\link[=Survr]{Survr()}}, \code{\link[=gcmrec]{gcmrec()}}
}
