% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph.caltimes.R
\name{graph.caltimes}
\alias{graph.caltimes}
\title{Event chart of recurrent events in calendar time}
\usage{
graph.caltimes(
  data,
  var = NULL,
  effageData = NULL,
  sortevents = c("followup", "events", "none"),
  width = 2,
  lines = TRUE,
  max.subjects = 300,
  ...
)
}
\arguments{
\item{data}{A data frame with \code{id}, \code{time} and \code{event} variables (and
possibly covariates), or a legacy gcmrec list (see
\code{\link[=as_gcmrec_data]{as_gcmrec_data()}}).}

\item{var}{Optional variable used to colour the subjects, of the same
length as \code{nrow(data)}. Categorical variables colour by group;
continuous ones use a gradient.}

\item{effageData}{Optional effective age information; recurrences with
\code{perrepind == 0} (imperfect repair) are drawn as hollow triangles.}

\item{sortevents}{How to order the subjects along the vertical axis:
\code{"followup"} (default) by length of follow-up, \code{"events"} by number
of recurrences, or \code{"none"} to keep the order of the data. In earlier
versions this argument was accepted but ignored.}

\item{width}{Size of the plotting symbols.}

\item{lines}{Draw the follow-up segment of each subject?}

\item{max.subjects}{Maximum number of subjects to draw. With more, a
random sample of this size is taken (with a message), since the chart
becomes unreadable beyond a few hundred rows. \code{Inf} draws all.}

\item{...}{Ignored; kept for compatibility with earlier versions.}
}
\value{
A \code{ggplot} object.
}
\description{
Draws one row per subject with a segment covering its follow-up, a
point at each recurrence and a marker at the end of follow-up. It is
the first look you should take at recurrent event data: it shows at a
glance how many subjects recur, how often, and whether the events
cluster early or late.
}
\examples{
data(readmission)
graph.caltimes(readmission[readmission$id \%in\% 1:40, ])

# Coloured by a covariate, ordered by number of events
graph.caltimes(readmission[readmission$id \%in\% 1:40, ],
               var = readmission$sex[readmission$id \%in\% 1:40],
               sortevents = "events")
}
\seealso{
\code{\link[=mcf]{mcf()}} for the cumulative view of the same data.
}
