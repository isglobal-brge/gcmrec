% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.gcmrec.R
\name{plotBaseline}
\alias{plotBaseline}
\title{Compare the baseline functions of several models}
\usage{
plotBaseline(models, type.plot = "survival", conf.int = FALSE, level = 0.95)
}
\arguments{
\item{models}{A named list of \code{gcmrec} objects. The names are used in
the legend.}

\item{type.plot}{Curve to plot: \code{"survival"} (default) or \code{"hazard"}.}

\item{conf.int}{Draw confidence bands? Off by default, since
overlapping bands are hard to read.}

\item{level}{Confidence level of the bands.}
}
\value{
A \code{ggplot} object.
}
\description{
Draws the baseline survivor or cumulative hazard functions of two or
more fitted models on the same axes, which is the natural way to see
what an assumption (for instance perfect versus minimal repair)
changes.
}
\examples{
data(readmission)
mod.per <- gcmrec(Survr(id, time, event) ~ as.factor(dukes),
                  data = readmission, s = 3000, typeEffage = "perfect")
mod.min <- gcmrec(Survr(id, time, event) ~ as.factor(dukes),
                  data = readmission, s = 3000, typeEffage = "minimal")
plotBaseline(list(perfect = mod.per, minimal = mod.min))
}
\seealso{
\code{\link[=plot.gcmrec]{plot.gcmrec()}}
}
