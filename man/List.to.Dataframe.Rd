% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/List.to.Dataframe.R
\name{List.to.Dataframe}
\alias{List.to.Dataframe}
\title{Convert the legacy gcmrec list format to a data frame}
\usage{
List.to.Dataframe(data)
}
\arguments{
\item{data}{A list with elements \code{n} (number of subjects) and \code{subject}
(a list with one entry per subject).}
}
\value{
A data frame with one row per recurrence plus the censoring
time of each subject.
}
\description{
Converts a data set in the legacy list format (elements \code{n} and
\code{subject}, where each subject holds \code{k}, \code{tau}, \code{gaptimes} and a
covariate matrix; see the \link{hydraulic} and \link{GeneratedData} data sets)
into a data frame with columns \code{id}, \code{time}, \code{event} and \verb{covar.*}.
}
\examples{
data(hydraulic)
head(List.to.Dataframe(hydraulic))
}
\seealso{
\code{\link[=as_gcmrec_data]{as_gcmrec_data()}}, \code{\link[=gcmrec]{gcmrec()}}
}
