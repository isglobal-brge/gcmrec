% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gcmrec.R
\name{gcmrec}
\alias{gcmrec}
\title{Fit the general class of models for recurrent event data}
\usage{
gcmrec(
  formula,
  data,
  effageData = NULL,
  s,
  Frailty = FALSE,
  alphaSeed,
  betaSeed,
  xiSeed,
  tol = 10^(-6),
  maxit = 100,
  rhoFunc = "alpha to k",
  typeEffage = "perfect",
  maxXi = "Newton-Raphson",
  se = "Information matrix",
  cancer = NULL,
  keep.data = TRUE
)
}
\arguments{
\item{formula}{A formula object with a \code{\link[=Survr]{Survr()}} object as response (on
the left of the \code{~} operator) and covariate terms on the right.
Covariates are required.}

\item{data}{A data frame containing the variables named in the formula,
including \code{id}, \code{time} and \code{event} (see \code{\link[=Survr]{Survr()}}). Alternatively, a
list with elements \code{n} and \code{subject} in the legacy format described in
\code{\link[=List.to.Dataframe]{List.to.Dataframe()}}; it is converted internally via
\code{\link[=as_gcmrec_data]{as_gcmrec_data()}}.}

\item{effageData}{Optional list with effective age information per
subject (elements \code{intercepts}, \code{slopes}, \code{lastperrep}, \code{perrepind},
\code{effagebegin}, \code{effage}). If \code{NULL}, effective ages are generated under
the perfect or minimal repair model according to \code{typeEffage}.}

\item{s}{A selected calendar time.}

\item{Frailty}{Logical. If \code{TRUE}, the model with gamma frailties is
fitted via EM.}

\item{alphaSeed, betaSeed, xiSeed}{Initial values for \eqn{\alpha},
\eqn{\beta} and \eqn{\xi}.}

\item{tol}{Tolerance of the maximization procedures.}

\item{maxit}{Maximum number of iterations of the maximization procedures.}

\item{rhoFunc}{Function \eqn{\rho(k; \alpha)} for the effect of
accumulating event occurrences: \code{"alpha to k"} for
\eqn{\rho(k; \alpha) = \alpha^k} (default) or \code{"Identity"} for
\eqn{\rho(k; \alpha) = 1}. Partial matching is allowed.}

\item{typeEffage}{Effective age model: \code{"perfect"} (perfect repair,
default) or \code{"minimal"} (minimal repair). Partial matching is allowed.}

\item{maxXi}{Maximization method for the marginal likelihood of
\eqn{\xi}: \code{"Newton-Raphson"} (default) or \code{"Brent"}.}

\item{se}{Standard error estimation: \code{"Information matrix"} (default) or
\code{"Jacknife"} (leave-one-out; can be time consuming).}

\item{cancer}{Optional character vector with the response achieved after
each treatment for the cancer model of González et al. (2005), coded
\code{"CR"}, \code{"PR"} or \code{"SD"}. See the \link{lymphoma} data set.}

\item{keep.data}{Keep the prepared data in the fitted object? It lets
\code{\link[=anova]{anova()}} refit the model without the original data, at the cost of a
larger object. Set to \code{FALSE} for very large data sets.}
}
\value{
An object of class \code{gcmrec} with components including \code{coef}
(estimated \eqn{\alpha} and \eqn{\beta}), \code{var} (covariance matrix),
\code{loglik}, \code{Xi} and \code{frailties} (frailty model only), \code{Lambda} and
\code{Survival} (baseline estimates at the distinct effective ages
\code{diseff}). Methods are provided for \code{\link[=print]{print()}}, \code{\link[=summary]{summary()}}, \code{\link[=plot]{plot()}},
\code{\link[=lines]{lines()}}, \code{\link[=coef]{coef()}}, \code{\link[=vcov]{vcov()}} and \code{\link[=logLik]{logLik()}}.
}
\description{
Fits the general semiparametric model for recurrent events proposed by
Peña and Hollander (2004). This class of models incorporates an effective
age function which encodes the changes that occur after each event
occurrence (such as the impact of an intervention), allows for the
modelling of the impact of accumulating event occurrences on the unit,
admits a link function in which the effect of possibly time-dependent
covariates is incorporated, and allows the incorporation of unobservable
frailty components which induce dependence among the inter-event times
for each unit.
}
\details{
Estimation with frailties is implemented using an
expectation-maximization (EM) algorithm. The marginal likelihood for
\eqn{\xi} is maximized either by Newton-Raphson or by Brent's method,
re-parameterizing \eqn{\xi^{*} = \log(\xi)} to avoid negative estimates.
Iteration terminates when successive values of \eqn{\xi / (1 + \xi)}
differ by no more than \code{tol}. Estimation under the frailty model can be
slow for large data sets.
}
\examples{
data(readmission)

# Perfect repair model with rho(k; alpha) = alpha^k
mod <- gcmrec(Survr(id, time, event) ~ as.factor(dukes),
              data = readmission, s = 3000)
mod

# Minimal repair model
mod.min <- gcmrec(Survr(id, time, event) ~ as.factor(dukes),
                  data = readmission, s = 3000, typeEffage = "min")

# Legacy list format converted internally
data(hydraulic)
gcmrec(Survr(id, time, event) ~ covar.1 + covar.2,
       data = hydraulic, s = 4753)

\donttest{
# Model with frailties (EM algorithm)
mod.fra <- gcmrec(Survr(id, time, event) ~ as.factor(dukes),
                  data = readmission, s = 3000, Frailty = TRUE)

# Cancer model with effective age given by treatment response
data(lymphoma)
mod.can <- gcmrec(Survr(id, time, event) ~ as.factor(distrib),
                  data = lymphoma, s = 1000, cancer = lymphoma$effage)
}
}
\references{
Peña, E. and Hollander, M. (2004). Models for recurrent events in
reliability and survival analysis. In \emph{Mathematical Reliability: An
Expository Perspective} (Chapter 6, pp. 105--123). Kluwer Academic
Publishers.

González, J.R., Peña, E. and Slate, E. (2005). Modelling intervention
effects after cancer relapses. \emph{Statistics in Medicine}, 24(24),
3959--3975.

Gail, M., Santner, T. and Brown, C. (1980). An analysis of comparative
carcinogenesis experiments based on multiple times to tumor.
\emph{Biometrics}, 36, 255--266.
}
\seealso{
\code{\link[=Survr]{Survr()}}, \code{\link[=addCenTime]{addCenTime()}}, \code{\link[=graph.caltimes]{graph.caltimes()}}
}
