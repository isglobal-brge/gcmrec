% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lines.gcmrec.R
\name{lines.gcmrec}
\alias{lines.gcmrec}
\title{Add gcmrec baseline curves to a base graphics plot}
\usage{
\method{lines}{gcmrec}(x, type.plot = "survival", ...)
}
\arguments{
\item{x}{An object of class \code{gcmrec}.}

\item{type.plot}{Curve to add: \code{"survival"} (default) or \code{"hazard"}.}

\item{...}{Further graphical parameters passed to \code{\link[graphics:lines]{graphics::lines()}}.}
}
\value{
Invisibly, \code{x}.
}
\description{
\strong{[Deprecated]}

Adds the baseline curve of a fitted model to an open \strong{base graphics}
plot. Since version 2.0 \code{\link[=plot.gcmrec]{plot.gcmrec()}} returns a \code{ggplot} object, so
\code{lines()} can no longer add a curve to it. Use \code{\link[=plotBaseline]{plotBaseline()}} with a
list of models instead, which draws all of them on the same axes with a
legend.
}
\examples{
data(readmission)
mod.per <- gcmrec(Survr(id, time, event) ~ as.factor(dukes),
                  data = readmission, s = 3000, typeEffage = "perfect")
mod.min <- gcmrec(Survr(id, time, event) ~ as.factor(dukes),
                  data = readmission, s = 3000, typeEffage = "minimal")

# Recommended: both curves in one call
plotBaseline(list(perfect = mod.per, minimal = mod.min))
}
\seealso{
\code{\link[=plotBaseline]{plotBaseline()}}, the recommended replacement.
}
