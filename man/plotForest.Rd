% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotForest.R
\name{plotForest}
\alias{plotForest}
\title{Forest plot of the hazard ratios}
\usage{
plotForest(
  x,
  level = 0.95,
  labels = NULL,
  sort = FALSE,
  title = "Hazard ratios",
  subtitle = NULL
)
}
\arguments{
\item{x}{An object of class \code{gcmrec}.}

\item{level}{Confidence level of the intervals.}

\item{labels}{Optional named character vector to relabel the
covariates: \code{c(sex = "Female vs male")}. Names are matched against
the coefficient names.}

\item{sort}{Order the covariates by hazard ratio instead of keeping the
order of the model?}

\item{title, subtitle}{Plot title and subtitle; \code{NULL} for none.}
}
\value{
A \code{ggplot} object.
}
\description{
Draws the exponentiated covariate coefficients of a fitted \code{\link[=gcmrec]{gcmrec()}}
model with their confidence intervals, on a logarithmic axis with a
reference line at 1. This is the figure that usually accompanies the
table returned by \code{\link[=summary.gcmrec]{summary.gcmrec()}}.
}
\examples{
data(readmission)
mod <- gcmrec(Survr(id, time, event) ~ as.factor(dukes) + sex,
              data = readmission, s = 3000)
plotForest(mod)

# Readable labels for a report
plotForest(mod, labels = c("as.factor(dukes)2" = "Dukes C vs A-B",
                           "as.factor(dukes)3" = "Dukes D vs A-B",
                           "sex" = "Female vs male"))
}
\seealso{
\code{\link[=summary.gcmrec]{summary.gcmrec()}}
}
