% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.gcmrec.R
\name{plot.gcmrec}
\alias{plot.gcmrec}
\title{Plot the baseline functions of a gcmrec model}
\usage{
\method{plot}{gcmrec}(x, type.plot = "survival", conf.int = TRUE, level = 0.95, ...)
}
\arguments{
\item{x}{An object of class \code{gcmrec}.}

\item{type.plot}{Curve to plot: \code{"survival"} (default) or \code{"hazard"}.
Partial matching is allowed.}

\item{conf.int}{Draw the confidence band?}

\item{level}{Confidence level of the band.}

\item{...}{Ignored; kept for compatibility with the generic.}
}
\value{
A \code{ggplot} object, which can be further styled with the usual
\code{ggplot2} syntax.
}
\description{
Plots the estimated baseline survivor function or the baseline
cumulative hazard of a fitted \code{\link[=gcmrec]{gcmrec()}} model as a step function over
the effective age, with a pointwise confidence band.
}
\details{
The band is built from the Aalen variance of the cumulative hazard,
transformed to the log scale for the hazard and to the log-log scale
for the survivor function, so that the limits stay positive and inside
\eqn{[0, 1]} respectively (the default transformation of
\code{\link[survival:survfit]{survival::survfit()}}).
}
\examples{
data(readmission)
mod <- gcmrec(Survr(id, time, event) ~ as.factor(dukes),
              data = readmission, s = 3000)
plot(mod)
plot(mod, type.plot = "hazard", level = 0.99)

# It is a ggplot, so it can be restyled
plot(mod) + ggplot2::labs(title = "Colorectal cancer readmissions")
}
\seealso{
\code{\link[=plotBaseline]{plotBaseline()}} to compare several models, \code{\link[=plotPredict]{plotPredict()}}
for covariate-specific curves.
}
